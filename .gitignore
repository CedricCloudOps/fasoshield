.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
*.db
.env
.DS_Store
samples/
quarantine/
# Anchored to the repo root: the Python app's runtime data dir (PROJECT_ROOT/data).
# A bare "data/" also matched the Android source package bf.fasoshield.agent.data,
# silently excluding it from git.
/data/
# Kotlin build daemon working dir.
.kotlin/
# Page renders used to proof-read the presentation PDF.
docs/presentation/_preview/

# Documents generes. Les PDF ne sont jamais versionnes : le dossier de
# presentation est reconstruit par docs/presentation/gen_pdf.py et publie en
# artefact de CI, et la feuille de route fait foi dans docs/ROADMAP.md.
*.pdf
